@import url(http://fonts.googleapis.com/css?family=Oswald:700);
@import url(http://fonts.googleapis.com/css?family=Shadows+Into+Light+Two);

body {
  background: #D0D4D8 url(ponto-de-onibus.jpg) fixed no-repeat;
  background-size: 100% auto;
  padding: 150px 10% 0px 10%;
  font-family: 'Liberation Sans', Arial, sans-serif;
  font-family: 'Shadows Into Light Two', cursive;
  font-size: 16px;
  color: #000;
}

h1 {
  font-family: 'Oswald', sans-serif;
  font-size: 80px;
  font-weight: bold;
  margin: 0px;
  padding: 0px 30px;
  color: #000;
  background: rgba(255,230,0,0.5); /*#FD0*/
  text-shadow: 0 0 30px #FD0;
  position: fixed;
  right: 0px;
  top: 0px;
}
.lt1050 h1 {
  font-size: 70px;
}
.lt900 h1 {
  font-size: 60px;
}
.lt750 h1 {
  font-size: 50px;
}
.lt650 h1 {
  font-size: 40px;
}

.info {
  font-family: 'Oswald', sans-serif;
  background: #393;
  color: #FFF;
  border: 15px solid #EEE;
  font-size: 24px;
  float: left;
  text-align: center;
  padding: 10px;
  border-radius: 25px;
  box-shadow: 1px 1px 20px rgba(0,0,0,0.5), inset 0 0 20px rgba(0,100,0,0.5);
  position: relative;
}
.info i {
  font-size: 60px;
  font-weight: bold;
  display: block;
  line-height: 24px;
}

h2 {
  font-family: 'Oswald', sans-serif;
  font-size: 24px;
  font-weight: normal;
  margin: 25px 0 0px 0;
  padding: 15px;
  color: #111;
  cursor: pointer;
  background: #FD0;
  border: 20px solid #222;
  border-radius: 35px;
  text-align: center;
  box-shadow: 1px 1px 50px rgba(255,255,255,0.4), inset 0 0 30px rgba(100,60,0,0.5);
}

h2 b, h2 strong {
  background: url(spray.png) 50% 50% repeat-x; rgba(255,240,200,0.5);
  height: 0.5em;
  padding: 0px 0px;
  border-radius: 20px;
}

.topic {
  position: relative;
  width: 70%;
}

.topic:nth-child(2n+1), .topic:nth-child(odd) {
  float: left;
  text-align: left;
}

.topic:nth-child(2n), .topic:nth-child(even) {
  float: right;
  text-align: right;
}

.topic div {
  background: #DDD url(ricepaper2.png);
  box-shadow: 1px 1px 15px rgba(30,20,0,0.4),
              1px 1px 0px rgba(60,30,0,0.1),
              inset 0 0 65px rgba(120,40,0,0.4);
  padding: 10px 20px;
  letter-spacing: 1px;
  float: right;
  margin: -30px -15px 0 0;
  width: 90%;
}

/*
.topic p:first-letter {
  font-size: 150%;
  font-weight: bold;
  color: #721;
}
*/

#footer {
  margin: 0% -12%;
  padding-top: 210px;
  height: 80px;
  background: url(salvador-card.jpg) 0% 0% no-repeat;
  clear: both;
  text-align: center;
  font-family: 'Finger Paint';
  color: rgba(0,0,0,0.3);
  text-shadow: 1px 1px 0 rgba(255,255,255,0.5);
}

#footer a {
  color: rgba(0,0,0,0.3);
}
